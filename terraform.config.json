{
  "scripts": {
    "description": "Enhanced Terraform deployment scripts",
    "categories": {
      "build": {
        "build": "Build Lambda functions",
        "build:local": "Build with watch mode for development"
      },
      "terraform": {
        "tf:init": "Initialize Terraform",
        "tf:plan:*": "Plan infrastructure changes for environment",
        "tf:apply:*": "Apply infrastructure changes for environment",
        "tf:destroy:*": "Destroy infrastructure for environment",
        "tf:validate": "Validate Terraform configuration",
        "tf:fmt": "Format Terraform files"
      },
      "deployment": {
        "deploy:local": "Full deployment to LocalStack",
        "deploy:dev": "Full deployment to development",
        "deploy:prod": "Full deployment to production"
      },
      "utilities": {
        "check:terraform": "Format and validate Terraform",
        "tf:output": "Show Terraform outputs",
        "tf:state:list": "List Terraform state resources"
      }
    }
  },
  "environments": {
    "local": {
      "description": "LocalStack development environment",
      "provider": "LocalStack",
      "autoApprove": true,
      "logLevel": "debug"
    },
    "dev": {
      "description": "AWS development environment",
      "provider": "AWS",
      "autoApprove": false,
      "logLevel": "info"
    },
    "prod": {
      "description": "AWS production environment",
      "provider": "AWS",
      "autoApprove": false,
      "logLevel": "warn"
    }
  }
}
